server:
  port: 8011
spring:
  application:
     # ribbon微服务之间的负载均衡不支持下划线起名
     # name: eureka_provider
     name: eureka-provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
